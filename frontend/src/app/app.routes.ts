import { Routes } from '@angular/router';
import { RegistroPontoComponent } from './features/ponto/registro-ponto.component';

export const routes: Routes = [
  { path: '', component: RegistroPontoComponent },
];
